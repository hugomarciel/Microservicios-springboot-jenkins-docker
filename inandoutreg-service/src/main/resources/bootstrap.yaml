spring:
  application:
    name: inandoutreg-service
  cloud:
    config:
      enabled: true
      uri: ${CONFIG_SERVER_URI}
  datasource:
    url: jdbc:mysql://localhost:3306/inandoutreg
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
