apiVersion: apps/v1
kind: Deployment
metadata:
  name: categorys-backend
  labels:
    app: categorys-backend
spec:
  selector:
    matchLabels:
      app: categorys-backend
  replicas: 1
  template:
    metadata:
      labels:
        app: categorys-backend
    spec:
      containers:
      - name: categorys-backend
        image: hugomarciel/categorys:latest
        ports:
        - containerPort: 8005
        env:
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: justifications-db
              key: host
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: justifications-db
              key: categorysdbname
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-user-credentials
              key: password
        - name: CONFIG_SERVER_URI
          valueFrom:
            configMapKeyRef:
              name: justifications-db
              key: configserver
        - name: EUREKA_SERVER_URI
          valueFrom:
            configMapKeyRef:
              name: justifications-db  # Nombre del ConfigMap que contiene la URL de CONFIG_SERVER_URI
              key: eurekaserver        
            # Aquí está la URL del Config Server
---
apiVersion: v1
kind: Service
metadata:
  name: categorys-backend
  labels:
    app: categorys-backend
spec:
  selector:
    app: categorys-backend
  ports:
    - nodePort: 30504
      protocol: TCP
      port: 8013
      targetPort: 8005
  type: NodePort
