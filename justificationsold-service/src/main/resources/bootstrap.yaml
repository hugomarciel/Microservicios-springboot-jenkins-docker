spring:
  application:
    name: justifications-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/justifications
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
