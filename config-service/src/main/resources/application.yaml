server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/hugomarciel/microsevices-data-project
          search-paths: config-data-local
  application:
    name: config-service
